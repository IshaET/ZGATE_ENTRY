projection;
strict ( 2 );
use draft;

define behavior for ZC_GATEENTRY_hdr_S //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action FetchT1;
  use action ref;
  use association _ADV_item1 { create; }
}

define behavior for ZC_GATEENTRY_ITM_S //alias <alias_name>
{
  use update;
  use delete;

  use association _ADV_HEAD1;
}