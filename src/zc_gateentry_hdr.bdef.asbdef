projection;
strict ( 2 );
use draft;

define behavior for ZC_GATEENTRY_hdr // alias ADV_HEAD//alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action CreateGrn;
  use action ref;
  use action FetchT;
  //  use action CreateGrn;
  use association _ADV_item { create; }
}

define behavior for ZC_GATEENTRY_ITM //alias ADV_item //alias <alias_name>
{

  use update;
  use delete;
  use action save0;
  use association _ADV_HEAD;

}