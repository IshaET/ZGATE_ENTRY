projection;
strict ( 2 );
use draft;

define behavior for ZC_GATEENTRY_hdr_R //alias <alias_name>
{
  use create;
  use update;
  use delete;

   use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _ADV_itemR { create; }
}

define behavior for ZC_GATEENTRY_itm_R //alias ADV_item //alias <alias_name>
{

  use update;
  use delete;

  use association _ADV_HEADr;

}